import arrow from '../../../images/arrow.png'

const Arrow = () => (
  <div style={{ height: '100%', display: 'flex', alignItems: 'center'}}>
    <img src={arrow} alt='arrow' style={{height: '40%'}}/>
  </div>
)

export default Arrow